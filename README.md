# Export-Spreedsheet-Data
Exporting excel data to mongoDB \

after cloning the project, run below commands :
npm install \
nodemon index.js or node index.js \
Go to the http://localhost:8000 in the browser \
